{% extends 'TSEPAdminBundle::admin.html.twig' %}

{% block content %}
    <h1>Profile creation</h1>

    <form action="{{ path('admin_profile_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_profile') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
